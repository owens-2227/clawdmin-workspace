# 2026-03-23

## Fixes
- Cron report job was posting to Paul's DM instead of #C0AL6FV5G1M — needed `delivery.to: "channel:C0AL6FV5G1M"` with `channel:` prefix
- LocalXpose tunnel: must use `--reserved-domain clawdmin.loclx.io` (Pro account), NOT `--subdomain clawdmin` (reserved subdomain error)
- Dashboard at `/Users/owen/Documents/openclaw-experiment/reddit-dashboard` wasn't running — started with `npm run dev`
- Gateway was offline this morning, both cron jobs failed (ENOTFOUND slack.com)

## Pain Points Scan
- Ran full 12-agent scan at ~4:15 PM PDT (delayed from 8 AM)
- ~165 new pain points added, 577 total in DB across 52 subreddits
- Agent results: maya 24, dave 24, nora 21, jess 20, owen 20, raj 20, elise 19, priya 17, ty 13, marcus 11, marco 7
- Marco-v timed out twice (Reddit blocking curl from this IP), had to scan manually
- Priya-k timed out but identified all points, manually submitted
- r/yinyoga is private, r/homerecording is private, r/CBTi is empty — need replacements
- Top themes: decision paralysis, financial anxiety, beginner overwhelm, mental health crossover

## Lessons
- Subagent timeout issue: agents struggle with Reddit JSON API when curl is blocked — need explicit instructions to use web_fetch tool + x-api-key header for dashboard POST
- loclx `--subdomain` vs `--reserved-domain` — always use reserved for Pro account
- Cron delivery `to` field format: `channel:C0AL6FV5G1M` not just `C0AL6FV5G1M`
