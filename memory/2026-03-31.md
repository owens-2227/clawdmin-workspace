# 2026-03-31

## Pain Points Scanner — Major Overhaul
- Paul noticed daily reports showing stale data — same pain points for days
- Root cause: Daily REPORT cron was working, but no SCAN cron existed. Scanner hadn't run since Mar 27.
- Created daily scan cron (7 AM PDT) that dispatches 12 subagents through AdsPower profiles

## First Scan Run (12 agents)
- 4 succeeded (maya-chen 11pp, jess-m 15pp, nora-p 4pp, owen-b 25pp partial)
- 8 timed out with zero output
- Root cause: Reddit's new SPA (shreddit) lazy-loads posts — only ~3 visible without scrolling
- Total: 878 → 1,023 pain points (+145)

## Scan Instructions Fix
- Rewrote BRAIN/scripts/scan-via-adspower.md with:
  - Explicit scroll-to-load logic (6 scrolls with 2s pauses)
  - JSON API fallback (`/hot.json`) if browser scraping fails
  - "Log after each subreddit" rule (not just at end)
  - 20-minute time budget guidance
- Test run with ty-m and marcus-j: both succeeded (27 + 21 pain points)
- Total now: 1,082 pain points

## Mongo Atlas Migration
- Migrated all 9 SQLite tables to Mongo Atlas (Cluster0, db: reddit_scanner)
- Connection: mongodb+srv://paul_db_user:***@cluster0.tdvafh8.mongodb.net
- Refactored entire Next.js app (17 files) from better-sqlite3 → mongodb native driver
- Build passes, all API endpoints verified working against Mongo
- SQLite file kept as backup

## Codex Fallback Models Added
- Added openai/gpt-5.3-codex (alias: codex53) and openai/gpt-5.2-codex (alias: codex52) as fallbacks
- Config: ~/.openclaw/openclaw.json → agents.defaults.model.fallbacks
- OpenAI API key was already in ~/.openclaw/agents/main/agent/auth-profiles.json (used for memory embeddings)

## Dashboard LaunchAgent
- Already existed at ~/Library/LaunchAgents/com.local.reddit-dashboard.plist
- Auto-starts on boot, auto-restarts on crash — no action needed

## Infrastructure Notes
- OpenClaw config file: ~/.openclaw/openclaw.json
- Auth profiles: ~/.openclaw/agents/main/agent/auth-profiles.json (has both Anthropic + OpenAI keys)
- Memory embeddings use OpenAI text-embedding-3-small
